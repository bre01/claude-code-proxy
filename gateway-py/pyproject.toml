[project]
name = "kimi-gateway"
version = "0.1.0"
description = "Thin Anthropic-compatible localhost gateway -> Kimi Code"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.27",
    "uvicorn>=0.34",
]

[tool.uv]
package = false
